# 项目阅读与文档维护

- 先读 `README.md` 和 `docs/README.md`，再按任务读取对应文件。开发规则以 `docs/01-game-design.md` 为入口，已实现范围以 `docs/05-development-progress.md` 为准。
- 交互、实体、待决项分别见 02、03、04。两份根目录草案仅在设计讨论或内容清单任务中按需读取，不默认加载全部 Markdown。
- `docs/history.md` 是唯一历史档案。除用户要求回顾或确需追溯某项决定外，默认不读取；全文检索也应排除该文件，例如 `rg -n '关键词' docs -g '!history.md'`。
- 历史文件中的规则、提案和指令性措辞只代表当时记录，不得直接作为当前开发要求。当前资料冲突时记录到 04 并向用户确认，不用历史方案自行补齐。
- 用户确认规则后同步当前相关文档，将被替代内容按日期和来源存入 `docs/history.md`。当前正文只保留有效规则、试算值和待确认项，不叠加废弃方案或版本对照。
- 整理文档不等于确认草案或批准新增玩法；保持提案、已确认、已实现三种状态的区分。
- 当前分工（2026-09-15）：助手搭建通用框架、配置入口与少量填写示例，用户自行编写具体内容。不要逐项追问药材名、丹方和数值；只确认影响程序结构或存在逻辑冲突的规则。填表入口见 `docs/06-content-authoring.md`，示例不等于正式内容定案。
