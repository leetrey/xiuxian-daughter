# 开发进度

更新日期：2026-09-18。

## 当前交付

当前在 D1 培养回合与 D2 洞天最小生产闭环上，保留作者可自行填写的物品、配方骨架，并按“不能再像 HTML 看板”的反馈改为场景式游戏界面。不扩充小游戏，也不声称已完成包含剧情、比赛和冒险的完整双路线。

实现沿用 Godot 常规目录，新增可替换的背景、三阶段立绘和建筑图集。以下仅记录当前可运行范围。

本轮继续 D3：把剧情配置接到回合始末，加入线性对话、完成记录、物品费用与奖励、活动解锁。采用前次提出且用户要求继续的基础方案；父亲恢复、继承经济成本与并发组仍待确认和实现。

| 已实现 | 范围 |
|---|---|
| 配置 | items.json 放物品；recipes.json 放种植采集加工；cultivation.json 放培养与初始库存；cave.json 放建筑配方引用、御灵与地块 |
| 内容校验 | 检查物品分类、配方投入产出、跨表引用、初始库存及建筑产物限制；错误指出文件和字段；validate_content.tscn 为作者独立检查入口 |
| 剧情 | story.json 配置条件、前置、文本/立绘和物品结算；回合开始及生产入库后播放，末句后完成，同一逻辑节点本局只完成一次 |
| 活动解锁 | 活动可声明 requires_unlock；剧情解锁可即时或下一回合生效，仍需满足原有成长阶段门槛 |
| 属性 | 16 项 A、四维汇总、A/B 分离计算，永久成长无固定上限 |
| 培养 | 默认三槽，选择、替换、排序、清空；代码支持配置四或五槽 |
| 自由活动 | 独立精力、拜访乡邻试作活动、清心草实际库存消耗 |
| 压力 | 每槽按当前压力判定，2.5/1/0.8 倍率，逐槽增长压力 |
| 回合 | 确认后不可再做自由活动或重复结算；结果页继续；新回合精力回满、压力不恢复 |
| 阶段 | 内部 60 回合与 12/24/24 分段；中后期吐纳开放；不显示具体年月或年龄 |
| 界面 | 全屏家园、按阶段切换的女儿立绘、边缘 HUD；属性、自由活动、培养与札记按需展开；点击女儿可查看属性 |
| 洞天 | 全屏山谷、等距投影的 8×6 逻辑格，入口与道路、七种建筑图集、建造和免费移动；选中时展开详情 |
| 派工 | 连通民居提供容量；每只御灵只负责一处；工作槽数支持配置 0 至 3 |
| 生产 | 灵田可选清心草或青蕴草示例；明确选择任务、预览投入产出，培养后统一扣料与入库，不使用同批新产物 |
| 乘区 | 御灵适应性、景观距离递减、同词条相加、不同词条独立相乘 |
| 回流培养 | 清心草可直接减压，也可隔回合加工成清心丹再使用 |
| 结算提示 | 有生产能力但未安排任务的建筑会提示，可返回洞天或明确继续 |

尚未接入洞天剧情扩张与高级图纸解锁、父亲恢复、御灵招募、伙伴、冒险、战斗、比赛、继承选择、心魔剧情、回合末天赋、存档和正式结局。当前结束只表示培养回合耗尽，不会给出虚构的完整人生结局。

初始洞天直接开放所有试作建筑、配方和两只待命御灵，便于验证玩法，不是正式开局剧情。新增家园和山谷背景、三阶段透明立绘、八格建筑图集均为视觉样例，不是角色及建筑设定定稿；洞天样例是明亮场景，未制作 T1 夜间教学或季节变体。旧背景文件保留。Lucide 工具图标与许可证放在 assets/icons，生成素材来源及替换格式见 assets/README.md。

没有装备获取与换装 UI，三件装备贡献目前默认空，A/B 接口用测试数据验证。关闭程序会丢失本次试玩进度。

## 试作默认值

以下为运行这个小切片而选择的临时值，不替代主体设计中的待决项：

| 配置 | 当前值 | 说明 |
|---|---|---|
| schedule_slots | 3 | 可改 4/5 后重启；这不是已经完成的继承点入口 |
| require_full_schedule | true | 默认填满再确认；false 可留部分空槽，但仍要求至少一项 |
| allow_repeat | true | 默认允许重复同一培养，无重复收益递减 |
| max_energy | 10 | 试算值，不是已批准精力上限 |
| initial_attribute | 8 | 每项细分初值 |
| initial_pressure | 12 | 试算值 |
| rounding | round | 每项细分增长独立四舍五入；可改 floor，实际期望会受取整影响 |
| 初始库存 | 木材 36、石料 24、清心草 3 | 可在 cultivation.json 调整；洞天可继续生产补给 |
| 拜访乡邻 | 精力 2，人缘 +2，礼节 +1 | 自由活动占位定义，不是最终活动内容 |
| 清心草使用 | 消耗 1，压力 -20，精力成本 0 | 减压与精力成本均为试算，可调；零压力时不消耗 |
| 清心丹使用 | 消耗 1，压力 -50，精力成本 0 | 加工链试作物品；和草共用自由活动的库存与减压逻辑 |

日程材料成本在确认前聚合检查，通过后按顺序扣除；总量不足时整批不执行，不先结算前几槽。示例基础日程暂不收材料。此处理只是 C01 的试作选择；C02/C03 的临时实现如下，不视为已定案。

培养 RNG 只用于当前运行的培养结果，支持测试设置固定种子；不是已经完成的存读档或冒险种子策略。

## 洞天试作口径

本轮已经询问四向道路、缺料与分料顺序是否调整，尚未收到新的规则决定。以下先用于最小闭环，正式规则仍可在试玩后修改。

### 配置与数值

| 字段或内容 | 当前值与用途 |
|---|---|
| width / height | 8 / 6；坐标从左上角 (0, 0) 开始 |
| entrance / initial_roads | 入口 (0, 3)，其右侧五格为初始路；调整时需保持在地块内 |
| buildings | 稳定 ID 到建筑定义；name、category、size、costs、worker_slots、recipes 等 |
| categories | housing 民居、production 生产、manufacturing 制造、landscape 景观 |
| items.json items.category | resource 基础资源、herb 药材、product 制品；药材不归入基础资源 |
| recipes.json recipes | inputs 与 outputs 为物品 ID 到正整数数量；空 inputs 表示不耗材料，每栋每回合执行一批 |
| buildings.*.recipes | 建筑菜单中的配方 ID 列表，引用独立配方表 |
| buildings.*.output_categories | 有任务的建筑必填，约束全部产物；灵田为 herb，林场采石场为 resource，丹炉为 product |
| workers | 御灵 ID、名称与各 work_type 的适应性；默认青禾、玄砂 |
| 适应性映射 | 一只御灵倍率 = 1 + 适应性 / 100；多只分别相乘，未派工倍率为 1 |
| bonus | affix 同词条归组；work_types 决定适用建筑；peak 相邻满额加成；radius 衰减范围 |
| distance_metric | manhattan 为曼哈顿距离，可改 chebyshev；取两建筑占地格之间的最近距离 |
| output_rounding | floor，逐建筑逐物品向下取整；可改 round |

默认示例配方：灵田选择产 4 清心草或 4 青蕴草，林场产 4 木，采石场产 4 石，丹炉消耗 4 清心草产 2 清心丹。青蕴草尚无正式加工用途，不自动采纳通用辅材提案。实际产出再乘御灵、景观倍率，材料成本不随倍率增加。竹舍容量 2；所有试作生产建筑为一个工作槽，尚无中大型内容，但规则与界面支持 2/3 槽。

景观贡献计算：`peak * max(0, 1 - (最近距离 - 1) / radius)`。相邻距离 1 满额；距离达到 `radius + 1` 时为零。相同 affix 的贡献先相加，再以 `1 + 合计贡献` 形成该乘区，不同词条之间相乘。

例如灵田基础 4，青禾适应性 50，两座相邻稻草人各加丰收 0.5，一座相邻灵泉加灵气 0.25：`4 * 1.5 * (1 + 0.5 + 0.5) * (1 + 0.25) = 15`。

### 管理与生产

- 上下左右相连且能到入口的道路才有效，斜角不算。民居、生产与制造建筑至少一个占地格邻接有效道路；景观不需要道路。
- 可以预先建在未连通处，但生产停工，民居不提供容量。无施工等待，当回合连通并安排任务即可生产。
- 派工时自动视为入住，取消工作即回待命池；暂不做单独入住界面。这是对正式“收藏与入住分开”的临时简化。
- 移动或断路若使民居容量低于已分配御灵数，整次操作拒绝，需先取消部分派工。不会自动驱逐或偷偷取消工作。
- 建筑、道路不消耗自由精力或培养槽。道路与移动免费；没有拆除、退款、卖出、旋转和拖拽施工。
- 建筑 ID 创建后不变；移动保留 ID、任务和御灵，任务跨回合保留。未派御灵但已经选配方的建筑仍按基础产出，不计入“未安排”提醒。
- 培养先扣自己的材料。生产阶段再按建筑 ID 从小到大分配已有库存；缺料整单停产，不部分扣费，也不保留材料给下一回合。
- 预览使用同一 forecast 函数，并先减去日程草稿的材料成本。预览不扣费、不锁库存；建造、服药、调整日程后会重新计算。
- 分配投入时只减预算，不加入任何本批产出。全部计算后统一扣料、统一入库，避免同批加工链；随后才检查回合末剧情。父亲升级经济规则尚未接入。
- 确认日程前提示未安排生产的建筑；确认仍然结算才会推进。结果页禁止管理及消耗物品，下一回合恢复操作。

## 剧情播放与结算

- `story.json` 使用版本号 1 与节点数组；开局可读草药对话支线，永久属性 A 总和超过 1000 后可读突破主线，完成示例奖励 1 清心丹。不是正式内容定案，也不替代 T1/T2/T3 教学。
- 主线必须提供 `0/1/2` 三份表现，共用逻辑 ID；支线目前支持公共 `default` 和可选阶段覆盖，不据此决定正式支线变体数量。
- 条件支持回合范围、允许成长阶段、永久属性 A 总和严格大于门槛、真实库存持有数量；所有条件同时满足。库存条件不扣材料，预测产出不满足持有条件。
- `GameState.story` 保存本局完成节点、已检查的回合检查点、当前台词与本回合摘要；三阶段共用完成 ID，切阶段不重播。重新开始清空，关闭程序不保存。
- `turn_start/turn_end` 内先主线后支线，各自按 `order` 升序、同值按 ID 排序。读完后重新判断同类候选；不固定每回合一条。支线新满足的主线留给下一检查点，不逆向回跳。
- `STORY` 阶段阻止日程、自由活动、洞天操作和下一回合。最后一句确认前再次整体检查条件与成本，费用不足不部分扣费或发奖；旧 ID/行号请求无效。正常播放期间资源不能被其他操作修改。
- 物品费用在 `costs`，物品奖励在 `rewards.items`。只有实际完成才提交，不用自身奖励支付自身成本。报告保留生产原始收成与剧情实际变动，库存体现两者的净结果。
- 活动的 `requires_unlock` 默认为 false；设 true 后由 `rewards.activities` 解锁。`unlock_timing` 默认 next_turn，可设 immediate；待生效记录独立保存，回合开始剧情前生效。尚无可试玩的土地、图纸和继承成本逻辑，不能用这里的普通 costs 代替父亲恢复经济条件。
- 对话复用家园场景和已有立绘，下方整宽文本区，长文可滚动。末句显示物品与解锁变化；回合末读完自动返回札记。字段检查包含成本、奖励物品及活动引用；不声称可以证明任意剧情图必然可达。
- `validate_content.tscn -- --story-preview` 仍是只读打印，不完成剧情。仅增加普通 story_flow.gd、story_ui.gd 和 story.tscn，没有新增 Autoload 或专用编辑器。

尚未实现：分支选择、并发/互斥组仲裁、独立任务日志、历史对话回看、父亲升级、继承免重复费用和存档。正式内容与剩余 C04 边界仍需继续收敛。

## 修改与阅读

- [07 开发与调试手册](07-development-handbook.md)：启动、VS Code 配合、按调用链读注释、修改内容/规则/UI、断点和测试。核心脚本已补充职责、关键流程与副作用边界注释，不改变玩法行为。
- [06 内容填写指南](06-content-authoring.md)：作者填写物品、种植、双原料加工、物品使用与验证的示例。
- [items.json](../data/items.json) 与 [recipes.json](../data/recipes.json)：独立物品和配方内容表。
- [cultivation.json](../data/cultivation.json)：调活动和数值，修改后重启加载。
- [cave.json](../data/cave.json)：调地块、建筑、可用配方引用、御灵、景观。
- [content_tables.gd](../scripts/content_tables.gd)：JSON 读取与内容表校验，不执行玩法，不新增 Autoload。
- [story_rules.gd](../scripts/story_rules.gd)：剧情校验、只读条件与表现选择；填写入口见 06 第 7 节，调试入口见 07。
- [story_flow.gd](../scripts/story_flow.gd)：检查点、逐句推进、完成提交和延后活动解锁；[story_ui.gd](../scripts/story_ui.gd) 只展示和提交推进请求。
- [ui_theme.gd](../scripts/ui_theme.gd)：共享配色、控件样式与图标资源，不参与游戏数值。
- [game_state.gd](../scripts/game_state.gd)：本局状态、A/B、阶段推进及核心配置校验。
- [schedule_manager.gd](../scripts/schedule_manager.gd)：草稿、校验、实际扣费和顺序培养。
- [cave_rules.gd](../scripts/cave_rules.gd)：布局、道路连通、唯一工作归属与批次生产，普通静态方法脚本。
- [main.gd](../scripts/main.gd)：原生 Godot 家园、三阶段立绘、浮层菜单、洞天导航、闲置弹窗与结果汇总。
- [cave_ui.gd](../scripts/cave_ui.gd)：列表、模式、任务、派工、产出详情；只提交操作，不自行计算收益。
- [cave_board.gd](../scripts/cave_board.gd)：等距投影、透明建筑图集和放置预览；绘制与鼠标命中共用坐标换算。
- [smoke_test.gd](../scripts/smoke_test.gd)：一份场景验证主干，可选真实窗口截图。

scenes/cave.tscn 是洞天根场景，普通 Control，场景后方为背景，前方为地块与管理控件。没有服务层框架、独立测试项目或新增管理器目录。保留 GameState 与 ScheduleManager 两个 Autoload，洞天没有新增全局单例。

GameState.cave 保存建筑数组、道路数组和御灵数组；工作归属仅保存于御灵的 building_id 与 slot。建筑不再维护另一份御灵列表。last_production 保存本次报告，last_production_turn 防止同回合重复入库。

## 本次验证

已在 Godot 4.7.1 运行：

- 无窗口核心测试，输出 SMOKE_TEST_OK。
- 真实窗口中触发选择器、确认与下一回合按钮的信号，验证相应状态变化。
- 通过方格鼠标输入处理建造，触发任务选择和派工；确认闲置弹窗能返回洞天；结算后管理控件锁定，下一回合恢复。
- 查看 1280×720 养成与洞天画面、闲置弹窗、生产报告，以及 960×640 五槽和洞天画面；属性栏具有滚动保护。
- 验证压力边界与理论期望、A/B 隔离、零精力培养、重复提交、锁定后操作、材料不足不部分扣费、阶段边界、3/4/5 槽和固定种子重现。
- 验证占地、重叠、越界、建材不足无副作用、四向道路、断路停产、民居容量保护、御灵唯一岗位、多御灵独立乘区、移动保留状态。
- 验证景观距离、同词条与跨词条乘区、两种距离与取整配置、生产预览无副作用、稳定分料、缺料整单停产、培养材料优先、跨回合加工和真实消耗减压。
- 验证常见配置缺字段、未知配方、零景观半径会返回错误；不表示完整 JSON 结构与数值类型的穷举校验。
- 2026-09-17：新增内容表、JSON 读取、整数数量、初始库存与跨表引用、灵田排除基础资源与制品（含副产物）的测试通过。
- 模拟仅修改数据新增药材和双原料丹方，验证菜单来源、预测、首次入库、跨回合加工、缺料不部分扣费；原有培养和洞天回归测试通过。
- 场景式界面在 1280×720、960×640、1600×900 运行并截图；默认先展示家园，三阶段立绘切换、透明素材、属性与自由活动菜单、三/五槽、结果、洞天七类建筑、青蕴草任务及闲置确认均走查。桌面窗口最小 960×540。
- 验证菜单收起与下一回合返回家园、实际自由行动扣精力、全部地块中心的投影命中、建造和派工、结算后锁定和下一回合恢复。窗口测试若中途脚本异常，不再误报完整通过。
- 2026-09-18：剧情配置、前置循环/引用、文本/立绘区域、严格 A 门槛、库存入库时点、阶段与回合范围、候选排序、完成排除与查询无副作用测试通过；原有培养和洞天回归通过。配置校验入口增加初始化完成检查，避免加载中断后误报成功。
- 2026-09-18：剧情执行集成测试通过，包括主支线顺序、末句才扣费发奖、旧请求拒绝、材料竞争重查、生产入库后条件、对话操作锁、阶段不重播、活动即时/下回合生效、新局清空与最终回合结束。
- 实际窗口走查三阶段对话、960×540/1280×720/1600×900 布局、长台词滚动、回车只推进一句、末句奖励预告、回合末返回札记、下一回合恢复操作。修复了重复刷新时结算报告重复追加的问题，并增加回归断言。

这不是完整 60 回合平衡测试，也不是全部键鼠、触屏、跨平台或异常配置结构的穷举测试。保存读取尚未实现，不能据此声称存档可靠。

## 下一段建议

按用户分工，具体药材、丹方、剧情文本与数值由作者依据 06 填写，不再逐项问答。下一段确认父亲恢复的经济解锁与继承成本拆分，接土地/图纸，再处理批准的存档边界。当前剧情闭环不代表 D3 全部完成，场景式界面也不等于最终美术定稿。
